#include<iostream>
#include<cstring>
using namespace std;
int main()
{
    int l,sum=0,i;
    string s;
    cin>>s;
    l=s.size();
    for(i=0 ; i<l ; i++)
        sum=sum+(s[i]-'0');
    cout<<sum;
    return 0;
}
