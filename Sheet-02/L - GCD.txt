#include<bits/stdc++.h>
using namespace std;
int main()
{
    int x,y,n,gcd,i;
    cin>>x>>y;
    n=min(x,y);
    for(i=1 ; i<=n ; i++)
    {
        if(x%i==0 && y%i==0)
        {
            gcd=i;
        }
    }
    cout<<gcd;
    return 0;
}
