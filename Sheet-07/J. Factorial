#include<bits/stdc++.h>
#define ll long long
using namespace std;
ll rec(int x)
{
    if(x==1) return 1;
    return x*rec(x-1);
}
int main()
{
    ll n,res;
    cin>>n;
    res=rec(n);
    cout<<res<<endl;
    return 0;
}
